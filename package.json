{
    "devDependencies": {
        "@wdio/cli": "^7.25.0",
        "jest": "^29.0.3"
    },
    "scripts": {
        "test": "jest",
        "multiple_execution": "for i in {1..1000}; do npx jest --silent || (echo `Failed` && break); done"
    },
    "dependencies": {
        "@katalon/testops-jest": "^1.2.2"
    }
}
